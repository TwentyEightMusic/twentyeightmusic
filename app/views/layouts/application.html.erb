<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Twenty Eight" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= filepicker_js_include_tag %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <script>
      var _roost = _roost || [];
      _roost.push(['appkey','1b4ab2107dd24f34a75eeb216a98acfa']);

      !function(d,s,id){
        var js, fjs = d.getElementsByTagName(s)[0];
        if(!d.getElementById(id)){
          js=d.createElement(s); js.id=id;
          js.src='//cdn.goroost.com/js/roost.js';
          fjs.parentNode.insertBefore(js,fjs);
        }
      }(document, 'script', 'roost-js');
    </script>
  </head>

  <body>
    <div id="logo">
      <h1><%= link_to "twenty eight", root_path %></h1>
    </div>

    <div id="picture">
      <%= image_tag "http://i.imgur.com/WE019mz.jpg", width: 400 %>
    </div>

    <%= yield %>
    <div id="sign-in">
      <% if signed_in? %>
        <%= link_to "sign out", session_path, method: :delete %>
      <% else %>
        <%= link_to "sign in", new_session_path %>
      <% end %>
    </div>
  </body>
</html>
